{
  "name": "cross-bridge-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only CROSS bridge route and stale-transfer sentinel.",
  "scripts": {
    "check": "node --check scripts/_json.mjs && node --check scripts/_args.mjs && node --check scripts/_config.mjs && node --check scripts/config.mjs && node --check scripts/check.mjs && node --check scripts/transfers.mjs",
    "smoke:read": "node scripts/config.mjs && node scripts/check.mjs && node scripts/transfers.mjs --demo"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {}
}
